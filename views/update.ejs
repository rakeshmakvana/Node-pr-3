<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update Volunteer</title>

  <!-- icon css -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.7/css/materialize.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />

  <!-- bootstrap css -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <!-- custom css -->
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>

  <div class="pb-4">
    <div class="formbg"></div>
    <div class="formbox">
      <div class="formcard">
        <div class="formcontent">
          <form method="post" action="/updateVolunter/<%= single.id %>">
            <div class="row">
              <div class="input-field col s12 my-2">
                <h4>Volunteer Registration Edit</h4>
                <p>Please complete all required fields*</p>
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <input name="name" type="text" placeholder="Full Name" value="<%= single.name %>" required />
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <input name="email" type="email" placeholder="Email Address" value="<%= single.email %>" required />
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <input name="phone" type="number" placeholder="Phone Number" value="<%= single.phone %>" required />
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <input name="work" type="text" placeholder="Your Work" value="<%= single.work %>" required />
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <input name="experience" type="text" placeholder="Your Experience" value="<%= single.experience %>"
                  required />
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <textarea name="summary" class="materialize-textarea" placeholder="Summary"
                  required><%= single.summary %></textarea>
              </div>
            </div>
            <div class="row">
              <div class="input-field col m6 s12">
                <button type="submit" class="waves-effect waves-light btn-large">
                  Update
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

</body>

</html>